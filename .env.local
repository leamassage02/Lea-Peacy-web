# Nodemailer SMTP Configuration
# Replace these values with your email provider settings (e.g., Gmail SMTP details)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password
